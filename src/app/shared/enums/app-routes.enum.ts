export enum AppRoutes {
    HOME = '',
    MAP = 'map',
    PULSES = 'pulses',
    PULSE = 'pulse/:id',
    HEATMAP = 'pulse/:id/heatmap',
    PRIVACY = 'privacy-policy',
    TERMS = 'terms-of-use',
    SUPPORT = 'support',
}